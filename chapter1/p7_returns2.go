package main

/*
import "fmt"

// Names : an example of an exported function that returns multiple values
// Note that the return values are named in the line below
func Names() (first string, second string) {
	first = "Foo"
	second = "Bar"
	return
}

func main() {
	n1, n2 := Names()
	fmt.Println(n1, n2)
}
*/
